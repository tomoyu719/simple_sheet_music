/// Enum representing different types of barlines.
enum BarlineType {
  /// No barline (not rendered).
  none,

  /// Single thin barline.
  single,

  /// Double thin barline.
  double,

  /// Final barline (thin + thick).
  final_;
}
